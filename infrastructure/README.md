# Mecorion Infrastructure

Здесь хранятся настройки окружения, а не бизнес-логика сервисов.

- `nginx` — будущий gateway для `/`, `/api` и `/media`;
- `postgres` — будущие SQL/init-настройки базы;
- `monitoring` — будущие логи, метрики и dashboards.
